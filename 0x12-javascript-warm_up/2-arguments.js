#!/usr/bin/node

const args = process.argv.slice(2);
if (args.length === 2) {
  console.log('No argumet');
} else if (args.length === 3) {
  console.log('Argument found');
} else {
 i console.log('Arguments found');
}
